<h2>確認</h2>

<div>下記の情報を登録します。</div>
<br />
<h3>プロジェクト名: <%= @project_name %></h3>

<% @tasks.each do | key, task| %>

<div>[タスク名]<%= task[:name]%>-<%= task[:finish] %></div>

<% end %>
<br />
<% form_tag({:action=>'complete', :project_id=>@project.id},{:multipart=>true}) do %>
<%= submit_tag "登録" %>
<% end %>
<% form_tag({:action=>'index', :project_id=>@project.id}) do %>
<%= submit_tag "戻る" %>
<% end %>