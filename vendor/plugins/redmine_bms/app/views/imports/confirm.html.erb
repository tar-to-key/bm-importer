<h2>登録内容確認</h2>

<div>下記の情報をプロジェクト登録します。</div>
<br />
<h3>プロジェクト名: <%= @bm_project[:name] %></h3>

<table class="list">
  <tr>
    <th>期日</th>
    <th>タスク名</th>
  </tr>
  <% @tasks.each do |task_k, task_v| %>
  <tr>
    <td><%= task_v[:finish] %></td>
    <td>
        <%= task_v[:name] %>
        <% if task_v[:issues].size > 0 then %>
          <ul>
          <% task_v[:issues].each do |issue_k, issue_v| %>
            <li><%= issue_v[:name] %></li>
          <% end%>
          </ul>
        <% end %>
    </td>
  </tr>
  <% end %>
</table>
<br />
<% form_tag({:action=>'complete'},{:multipart=>true}) do %>
<%= submit_tag "登録" %>
<% end %>
<% form_tag({:action=>'index'}) do %>
<%= submit_tag "戻る" %>
<% end %>
